# `HOST_NAME` Function

Returns the local computer name.

## Syntax

`HOST_NAME` `(` `)`

## Return Value

The computer hostname.

## Examples

- Prints the computer name.

    ```
    PRINT HOST_NAME();
    ```
